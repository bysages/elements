---
title: "气泡确认 Popconfirm"
description: "A question at the point of no return: the trigger opens a small anchored vessel carrying the message and two answers. Confirmation and cancellation are the caller's to act on — the panel closes either way. The default slot is the trigger; give it a single element (wrap a group in a span otherwise)."
navigation:
  section: "浮层"
---

`Actions` · `Message`

## Popconfirm

A question at the point of no return: the trigger opens a small
anchored vessel carrying the message and two answers. Confirmation
and cancellation are the caller's to act on — the panel closes
either way. The default slot is the trigger; give it a single
element (wrap a group in a span otherwise).

| Prop          | Type     | Default     | Description                           |
| ------------- | -------- | ----------- | ------------------------------------- |
| `message`     | `String` |             | The question the reader is answering. |
| `confirmText` | `String` | `"Confirm"` | —                                     |
| `cancelText`  | `String` | `"Cancel"`  | —                                     |

| Event     | Payload      |
| --------- | ------------ |
| `confirm` | `() => true` |
| `cancel`  | `() => true` |
