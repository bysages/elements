---
title: "AI 回复 AI Response"
description: "Markdown set on the paper. Rendering goes through `@tanstack/markdown`, whose defaults leave raw HTML and executable links inert — streaming-safe by construction. An optional highlighter re-inks fenced code; the component stays agnostic about which engine provides it."
navigation:
  section: "AI"
---

`Actions` · `Attachment` · `Attachments` · `Content` · `Conversation` · `Loader` · `Message` · `Prompt` · `PromptEnd` · `PromptFooter` · `PromptHeader` · `PromptLeading` · `PromptMain` · `PromptTrailing` · `ReasoningContent` · `Response` · `Root` · `Source` · `Sources` · `Textarea` · `ToolBody` · `ToolLabel` · `ToolStatus` · `Trigger`

## AiResponse

Markdown set on the paper. Rendering goes through
`@tanstack/markdown`, whose defaults leave raw HTML and executable
links inert — streaming-safe by construction. An optional
highlighter re-inks fenced code; the component stays agnostic about
which engine provides it.

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `content` | `String` |  | The markdown text to set on the paper — streamed in freely; raw HTML and executable links stay inert. |
| `highlighter` | `Function as PropType<(code: string, lang?: string) => string>` | `undefined` | Optional code-highlighting function re-inking fenced blocks; the component stays agnostic about which engine provides it. |
