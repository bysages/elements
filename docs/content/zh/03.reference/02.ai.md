---
title: "会话 Ai"
description: "A conversation column: Root is the log, Message carries a role, and the speaking parts — Response, Reasoning, Tool, Sources — part the stream. The interactive folds are the shared Collapsible wearing a `data-ai` marker, so the machine work is never ours. Parts stay agnostic of any client; consumers map their message format (e.g. the `UIMessage` parts re-exported below) onto these primitives."
---

`Actions` · `Content` · `Conversation` · `Loader` · `Message` · `Prompt` · `PromptFooter` · `ReasoningContent` · `Response` · `Root` · `Source` · `Sources` · `Textarea` · `ToolBody` · `ToolLabel` · `ToolStatus` · `Trigger`

## AiMessage

| Prop   | Type                                                    | Default       | Description |
| ------ | ------------------------------------------------------- | ------------- | ----------- |
| `role` | `String as PropType<"user" \| "assistant" \| "system">` | `"assistant"` | —           |

## AiResponse

| Prop          | Type                                                            | Default     | Description |
| ------------- | --------------------------------------------------------------- | ----------- | ----------- |
| `content`     | `String`                                                        |             | —           |
| `highlighter` | `Function as PropType<(code: string, lang?: string) => string>` | `undefined` | —           |

## AiReasoning

| Prop    | Type     | Default      | Description |
| ------- | -------- | ------------ | ----------- |
| `label` | `String` | `"Thinking"` | —           |

## AiTool

| Prop     | Type                                                                   | Default | Description |
| -------- | ---------------------------------------------------------------------- | ------- | ----------- |
| `name`   | `String`                                                               |         | —           |
| `status` | `String as PropType<"pending" \| "running" \| "completed" \| "error">` |         | —           |

| Slot      |
| --------- |
| `#input`  |
| `#label`  |
| `#output` |

## AiSource

| Prop   | Type     | Default | Description |
| ------ | -------- | ------- | ----------- |
| `href` | `String` |         | —           |

## AiAction

| Prop    | Type     | Default | Description |
| ------- | -------- | ------- | ----------- |
| `label` | `String` |         | —           |

## AiSuggestion

| Prop     | Type     | Default | Description |
| -------- | -------- | ------- | ----------- |
| `prompt` | `String` |         | —           |

| Event    | Payload                    |
| -------- | -------------------------- |
| `select` | `(prompt: string) => true` |

## AiPromptInput

| Prop          | Type      | Default            | Description |
| ------------- | --------- | ------------------ | ----------- |
| `modelValue`  | `String`  | `""`               | —           |
| `placeholder` | `String`  | `"Send a message"` | —           |
| `disabled`    | `Boolean` | `false`            | —           |

| Event               | Payload                   |
| ------------------- | ------------------------- |
| `update:modelValue` | `(value: string) => true` |
| `submit`            | `(value: string) => true` |
