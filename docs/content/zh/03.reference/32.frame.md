---
title: "画框 Frame"
description: "Frame, dressed in the paper-and-ink system: a sandboxed iframe whose body teleports the default slot and whose head accepts a `head` slot — styles the child renders must travel through that slot. The frame measures its content and grows to fit; the vessel's border and paper belong to the consumer, since an iframe carries no anatomy attributes for the core stylesheet to hook. The parts."
---

## Content

| Event     | Payload |
| --------- | ------- |
| `mount`   | `—`     |
| `unmount` | `—`     |
