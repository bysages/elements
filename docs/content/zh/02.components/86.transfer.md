---
title: "穿梭框 Transfer"
description: "Two ledgers and a crossing: items sit in the source column until the reader checks them and walks them across — and back, the same way. `modelValue` is the target column's value list; everything else in `data` stays on the left. `searchable` adds a filter line to each panel."
---

## 基础用法

<ComponentDemo name="transfer/basic"></ComponentDemo>

## 属性

### Transfer

| Prop         | Type                                | Default                      | Description |
| ------------ | ----------------------------------- | ---------------------------- | ----------- |
| `modelValue` | `Array as PropType<string[]>`       | `() => []`                   | —           |
| `data`       | `Array as PropType<TransferItem[]>` |                              | —           |
| `titles`     | `Array as PropType<string[]>`       | `() => ["Source", "Target"]` | —           |
| `searchable` | `Boolean`                           | `false`                      | —           |
| `disabled`   | `Boolean`                           | `false`                      | —           |
