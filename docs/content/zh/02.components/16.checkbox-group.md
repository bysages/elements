---
title: "复选框组 Checkbox Group"
description: 'One question, many answers: a labelled stack (or row) of the seal-cut checkboxes bound to a single array. Toggling a box adds or removes its value; the group itself is semantics (`role="group"`), the boxes stay the machine-driven originals.'
---

## 基础用法

<ComponentDemo name="checkbox-group/basic"></ComponentDemo>

## 属性

### CheckboxGroup

| Prop         | Type                                             | Default      | Description |
| ------------ | ------------------------------------------------ | ------------ | ----------- |
| `modelValue` | `Array as PropType<string[]>`                    | `() => []`   | —           |
| `options`    | `Array as PropType<CheckboxOption[]>`            |              | —           |
| `layout`     | `String as PropType<"vertical" \| "horizontal">` | `"vertical"` | —           |
| `disabled`   | `Boolean`                                        | `false`      | —           |
