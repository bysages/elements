---
title: "树选择 Tree Select"
description: "A tree behind a field: the control reads like an input, the vessel below walks the hierarchy, and one leaf click closes the deal. Single selection — the chosen label rides on the control, its value rides in `modelValue`."
---

## 基础用法

<ComponentDemo name="tree-select/basic"></ComponentDemo>

## 属性

### TreeSelect

| Prop          | Type                                  | Default     | Description |
| ------------- | ------------------------------------- | ----------- | ----------- |
| `modelValue`  | `String`                              | `undefined` | —           |
| `data`        | `Array as PropType<TreeSelectNode[]>` |             | —           |
| `placeholder` | `String`                              | `"Select…"` | —           |
| `disabled`    | `Boolean`                             | `false`     | —           |
