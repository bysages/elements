---
title: "全局提示 Toast"
description: "Toast, dressed in the paper-and-ink system: each notice rides the popup vessel while a pigment accents the title by type, and the machine's translate variables carry the slide. The parts — Toaster, Root, Title, Description, ActionTrigger, CloseTrigger, plus createToaster."
navigation:
  section: "浮层"
---

## 基础用法

<ComponentDemo name="toast/basic"></ComponentDemo>

## 属性

### ActionTrigger

A styled part — no props of its own; it takes the anatomy's shared styling.

### CloseTrigger

A styled part — no props of its own; it takes the anatomy's shared styling.

### Description

A styled part — no props of its own; it takes the anatomy's shared styling.

### Root

A styled part — no props of its own; it takes the anatomy's shared styling.

### Title

A styled part — no props of its own; it takes the anatomy's shared styling.
