---
title: "Input Group"
description: "Merged controls: attachments and the entry fused into one seal. The Root draws the single hairline and carries the group's focus halo; the Addon is a recessed cell for the reader's fixed words — a scheme, a unit, a quiet button — placed before or after the entry. Put our Input (or Textarea) inside and its own border and halo step aside in favor of the group's; the stylesheet does the merging, the wrapper adds no visuals of its own."
navigation:
  section: "Forms"
---

## Basic usage



<ComponentDemo name="input-group/basic"></ComponentDemo>

<ComponentDemo name="input-group/with-actions"></ComponentDemo>

## Props

### InputGroupRoot

Merged controls: attachments and the entry fused into one seal. The
Root draws the single hairline and carries the group's focus halo;
the Addon is a recessed cell for the reader's fixed words — a scheme,
a unit, a quiet button — placed before or after the entry. Put our
Input (or Textarea) inside and its own border and halo step aside in
favor of the group's; the stylesheet does the merging, the wrapper
adds no visuals of its own.

A styled part — no props of its own; it takes the anatomy's shared styling.

### InputGroupAddon

A styled part — no props of its own; it takes the anatomy's shared styling.
