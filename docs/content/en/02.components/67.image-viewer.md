---
title: "Image Viewer"
description: "A lightbox: the picture over a dimmed page, with a small toolbar beneath it. Zoom is the reader's hand (half to three times, clamped), a quarter turn at a time rotates, Escape and the scrim close — the dialog machine carries the modal part. `open` may stay with the caller; left undefined the viewer keeps it to itself."
navigation:
  section: "Media"
---

## Basic usage



<ComponentDemo name="image-viewer/basic"></ComponentDemo>

<ComponentDemo name="image-viewer/not-zoomable"></ComponentDemo>

## Props

### ImageViewer

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `src` | `String` |  | — |
| `alt` | `String` | `""` | — |
| `open` | `Boolean` | `undefined` | — |
| `zoomable` | `Boolean` | `true` | — |
