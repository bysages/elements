---
title: "Watermark"
description: "The paper bears its seal beneath the content: a canvas draws the text once into a tile (rotated, faint, crisp at the screen's pixel density) and the marks layer repeats it over the default slot. The seal redraws itself when a prop turns — and it never takes a pointer."
navigation:
  section: "Feedback"
---

`Content` · `Marks` · `Root`

## Watermark

The paper bears its seal beneath the content: a canvas draws the
text once into a tile (rotated, faint, crisp at the screen's pixel
density) and the marks layer repeats it over the default slot. The
seal redraws itself when a prop turns — and it never takes a
pointer.

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `content` | `String` |  | — |
| `opacity` | `Number` | `0.06` | — |
| `rotate` | `Number` | `-20` | — |
| `fontSize` | `String` | `"0.875rem"` | — |
