---
title: "AI Reasoning"
description: "The model's thought, folded by the shared collapsible in its quiet register: bare ink for a trigger, the thought on one hairline."
navigation:
  section: "AI"
---

`Actions` · `Attachment` · `Attachments` · `CodeBody` · `CodeCopy` · `Content` · `Conversation` · `Loader` · `Message` · `Prompt` · `PromptEnd` · `PromptFooter` · `PromptHeader` · `PromptLeading` · `PromptMain` · `PromptTrailing` · `ReasoningContent` · `Response` · `Root` · `Source` · `Sources` · `Textarea` · `ToolBody` · `ToolLabel` · `ToolStatus` · `Trigger`

## AiReasoning

The model's thought, folded by the shared collapsible in its quiet
register: bare ink for a trigger, the thought on one hairline.

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `label` | `String` | `"Thinking"` | The trigger's words — the fold arrives open under them. |
| `defaultOpen` | `Boolean` | `false` | The fold arrives open when set — rides the shared collapsible. |
