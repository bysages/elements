---
title: "Calendar"
description: "The date-picker's month grid, standing on the page without its popup: always open, the trigger gone, the vessel a quiet card. The grid itself is the date-picker's machinery — value, range selection, and focus included."
---

`Header` · `Root`

## Calendar

| Prop         | Type                       | Default     | Description                                                   |
| ------------ | -------------------------- | ----------- | ------------------------------------------------------------- |
| `modelValue` | `Array as unknown as any`  | `undefined` | Selected date(s) — an array, as the machine speaks in ranges. |
| `min`        | `Object as unknown as any` | `undefined` | —                                                             |
| `max`        | `Object as unknown as any` | `undefined` | —                                                             |

| Event               | Payload                     |
| ------------------- | --------------------------- |
| `update:modelValue` | `(_value: unknown) => true` |
