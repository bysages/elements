import { Format as ArkFormat } from "@ark-ui/solid/format";

/** Ark's Format, dressed in the paper-and-ink system: Intl formatting for
 * numbers, currency, bytes and relative time. The components render bare
 * text with no DOM of their own — typography flows from the token layer,
 * so there is no component stylesheet. The API is Ark's own — Number,
 * Byte, Time, RelativeTime. */
export const Format = ArkFormat;
